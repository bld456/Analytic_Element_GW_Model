function [ omega ] = Omega_total( z, model, s )
%OMEGA_TOTAL Summary of this function goes here
%   Detailed explanation goes here

omega  = 0;

for j = 1:model.nSinks
    if j == model
omega = omega + model.Sinks(j).omega(z,s
end
end

